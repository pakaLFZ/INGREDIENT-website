- u are going to build a one page website. u always use shadcn. u always use pnpm. u like Magic UI.
- for all pages u write, design it so that its optimized for phone applications with min screen width = 150 px. making sure touch, drag, click, etc are optmized for phone as well
- u do not need to test the website as I will do it for u

referebce color scheme:
- Cards: Dark zinc (bg-zinc-900) with gray borders (border-gray-700)
- Icons: Gray backgrounds (bg-gray-800) with light gray icons (text-gray-300)
- Text: White headings, gray-400 for body text
- Buttons: Dark zinc with gray borders, subtle hover states
- Lower contrast: Using gray-300, gray-400, gray-600, gray-700, gray-800 instead of pure black/white contrasts